# Google Apps Script (GAS) セットアップガイド

このガイドでは、申込情報をGoogleスプレッドシートに自動保存する仕組みを設定します。

---

## 🎯 GASでできること

1. **申込情報の自動保存**
   - 氏名、メールアドレス、適用価格をスプレッドシートに保存

2. **参加人数の自動カウント**
   - 現在の参加人数を取得
   - 早割適用状況を判定

3. **自動メール送信**（オプション）
   - 申込確認メールの自動送信

---

## 📋 セットアップ手順

### Step 1: Googleスプレッドシートを作成

1. [Google Sheets](https://sheets.google.com/) にアクセス
2. 「空白」をクリックして新しいスプレッドシートを作成
3. タイトルを「ホームページ作成セミナー申込管理」に変更

### Step 2: Apps Scriptエディタを開く

1. メニューバーから **「拡張機能」→「Apps Script」** をクリック
2. Apps Scriptエディタが新しいタブで開きます

### Step 3: スクリプトをコピー＆ペースト

1. エディタ内のコード（`function myFunction() { ... }`）を**すべて削除**
2. プロジェクトのルートにある `gas-script.js` の内容を**すべてコピー**
3. Apps Scriptエディタに**貼り付け**

**コピー元:**
```
/Users/kota5656/.cursor/projects/hp_seminar/gas-script.js
```

### Step 4: プロジェクト名を設定（オプション）

1. 左上の「無題のプロジェクト」をクリック
2. 「ホームページ作成セミナーGAS」など、わかりやすい名前に変更

### Step 5: デプロイ（公開）

1. 右上の **「デプロイ」→「新しいデプロイ」** をクリック
2. 「タイプの選択」で **「ウェブアプリ」** を選択
3. 以下の設定を行う：

   | 項目 | 設定値 |
   |------|--------|
   | **説明** | 「初回デプロイ」（任意） |
   | **次のユーザーとして実行** | **「自分（あなたのGoogleアカウント）」** |
   | **アクセスできるユーザー** | **「全員」** |

4. **「デプロイ」** をクリック

### Step 6: 権限の承認

初回デプロイ時に権限の承認が求められます：

1. **「アクセスを承認」** をクリック
2. Googleアカウントを選択
3. 「このアプリは確認されていません」と表示された場合：
   - **「詳細」** をクリック
   - **「（安全ではないページ）に移動」** をクリック
   - これは自分で作成したスクリプトなので安全です
4. **「許可」** をクリック

### Step 7: デプロイURLをコピー

1. デプロイが完了すると、**「ウェブアプリ」のURL**が表示されます
2. このURLを**コピー**してください
   - 例: `https://script.google.com/macros/s/AKfycby...../exec`

### Step 8: 環境変数に設定

コピーしたURLを `.env.local` ファイルに設定します：

1. プロジェクトのルートにある `.env.local` ファイルを開く
2. `NEXT_PUBLIC_GAS_URL=` の行を探す
3. コピーしたURLを貼り付け

**例:**
```env
NEXT_PUBLIC_GAS_URL=https://script.google.com/macros/s/AKfycby...../exec
```

### Step 9: 開発サーバーを再起動

環境変数を変更したので、開発サーバーを再起動します：

```bash
# Ctrl + C でサーバーを停止
npm run dev
```

---

## ✅ 動作確認

### テスト1: GETリクエスト（ブラウザ確認）

1. デプロイURLをブラウザのアドレスバーに貼り付け
2. Enterキーを押す
3. 以下のようなJSON形式のデータが表示されればOK：

```json
{
  "count": 0,
  "capacity": 30,
  "earlyBirdLimit": 10,
  "isEarlyBird": true,
  "currentPrice": 4980
}
```

### テスト2: 申込フォームからのテスト送信

1. http://localhost:3000 にアクセス
2. フォームに情報を入力
3. 「申し込む」ボタンをクリック
4. Stripe決済ページに遷移することを確認
5. Googleスプレッドシートを開く
6. 新しい行にデータが追加されていればOK！

**スプレッドシートの確認項目:**
- ✅ ヘッダー行が自動作成されている（青色の背景）
- ✅ タイムスタンプ、氏名、メールアドレス、価格が記録されている
- ✅ 決済ステータスが「未決済」になっている

---

## 📊 スプレッドシートの見方

| 列 | 内容 | 例 |
|---|------|-----|
| A | タイムスタンプ | 2025/11/6 14:30:00 |
| B | 氏名 | 山田 太郎 |
| C | メールアドレス | example@email.com |
| D | 適用価格 | ¥4,980 |
| E | 決済ステータス | 未決済 |

---

## 🔧 便利な機能

### 1. 参加人数の自動カウント

WebサイトのフォームがGASから参加人数を取得し、自動的に価格を切り替えます：

- **0〜9人**: 早割価格 ¥4,980
- **10人以上**: 通常価格 ¥6,980

### 2. 自動メール送信（オプション）

申込確認メールを自動送信したい場合：

1. `gas-script.js` の59〜86行目のコメントアウトを外す（`/*` と `*/` を削除）
2. Apps Scriptエディタで同じコードを更新
3. 「デプロイ」→「デプロイを管理」→「編集」→「バージョン：新バージョン」→「デプロイ」

### 3. 決済ステータスの手動更新

Stripe決済完了後、手動でステータスを更新する場合：

1. スプレッドシートを開く
2. 該当行のE列（決済ステータス）をクリック
3. 「決済完了」に変更

---

## 🔄 デプロイの更新方法

スクリプトを変更した場合の更新手順：

1. Apps Scriptエディタでコードを修正
2. **「デプロイ」→「デプロイを管理」** をクリック
3. 鉛筆アイコン（編集）をクリック
4. **「バージョン」→「新バージョン」** を選択
5. **「デプロイ」** をクリック
6. URLは変わらないので、`.env.local` の変更は不要

---

## 🐛 トラブルシューティング

### Q1: 「アクセスできるユーザー」が「全員」にできない

**A:** Googleアカウントの設定で外部共有が制限されている可能性があります。
- 組織管理者に確認してください
- または、個人のGoogleアカウントを使用してください

### Q2: スプレッドシートにデータが保存されない

**A:** 以下を確認してください：
1. デプロイURLが正しく `.env.local` に設定されているか
2. 開発サーバーを再起動したか
3. ブラウザの開発者ツール（F12）でエラーが表示されていないか

### Q3: 「このアプリは確認されていません」と表示される

**A:** これは正常な動作です。自分で作成したスクリプトなので安全です。
- 「詳細」→「（安全ではないページ）に移動」をクリックしてください

### Q4: 参加人数が正しくカウントされない

**A:** スプレッドシートの1行目がヘッダーになっているか確認してください：
- 1行目: ヘッダー（タイムスタンプ、氏名、メールアドレス、適用価格、決済ステータス）
- 2行目以降: データ

---

## 🔒 セキュリティに関する注意事項

### ✅ 安全な設定

1. **「次のユーザーとして実行」→「自分」**
   - スクリプトはあなたの権限で実行されます
   - あなたのGoogleアカウントでスプレッドシートにアクセスします

2. **「アクセスできるユーザー」→「全員」**
   - Webサイトからアクセスするために必要です
   - URLを知っている人のみがアクセスできます
   - スプレッドシートそのものは公開されません

### ⚠️ 注意点

- GASのURLは公開情報として扱われます
- ただし、スプレッドシート自体にはアクセスできません
- 不正なデータ送信を防ぐため、本番環境ではバリデーションの追加を推奨

---

## 📞 サポート

問題が解決しない場合は、以下の情報をお知らせください：

1. エラーメッセージ（スクリーンショット）
2. Apps Scriptのログ（「実行」→「実行ログを表示」）
3. ブラウザの開発者ツールのエラー（F12 → Console）

---

**作成日**: 2025年11月6日  
**最終更新**: 2025年11月6日

